= simple_form_for @position do |f|
  = f.input :name
  = f.input :career, as: :select, collection: @careers, value_method: :first,label_method: :second, include_blank: false, selected: @position.career
  = f.input :contract, as: :select, collection: @contracts, value_method: :first, include_blank: false, selected: @position.contract
  = f.input :remote
  = f.input :publish
  = f.input :city
  = f.input :state
  = f.input :summary
  = f.button :submit, class: 'btn btn-primary'